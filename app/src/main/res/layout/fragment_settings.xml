<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="viewmodel" type="com.example.chat_app.ui.settings.SettingsViewModel" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="?attr/actionBarSize">





    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >

            <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/passBtn"
                android:text="@string/add_key"
                app:layout_constraintBottom_toTopOf="@id/logoutButton"
                app:layout_constraintStart_toStartOf="parent"
                android:onClick="@{() -> viewmodel.addKeyPressed()}"/>

            <Button
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:id="@+id/rmpassBtn"
                 android:text="@string/remove_key"
                 app:layout_constraintBottom_toTopOf="@id/logoutButton"
                 app:layout_constraintEnd_toEndOf="parent"
                 android:onClick="@{() -> viewmodel.removeKeyPressed()}"/>



        <Button
                android:layout_width="120dp"
                android:layout_height="wrap_content"
                android:id="@+id/logoutButton"
                style="@style/ExtraBoldText"
                android:text="@string/logout"
                android:layout_marginTop="100dp"
                app:layout_constraintTop_toBottomOf="@id/linearLayout"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:backgroundTint="@color/textError"
                android:textSize="14sp"
                android:onClick="@{() -> viewmodel.logoutPressed()}"

                />
            <ImageView
                android:id="@+id/blurredUserImage"
                android:layout_width="0dp"
                android:layout_height="170dp"
                tools:src="@tools:sample/avatars"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:scaleType="centerCrop"
                app:bind_image_url_blur="@{viewmodel.userInfo.profileImageUrl}"
                />

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/circleProfileImage"
                android:layout_width="120dp"
                android:layout_height="120dp"
                app:civ_border_width="2dp"
                app:civ_border_color="@color/white"
                tools:src="@tools:sample/avatars"
                app:layout_constraintTop_toBottomOf="@id/blurredUserImage"
                app:layout_constraintBottom_toBottomOf="@id/blurredUserImage"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:bind_image_url="@{viewmodel.userInfo.profileImageUrl}"
                android:onClick="@{() -> viewmodel.profileImagePressed()}"/>

            <LinearLayout
                android:id="@+id/linearLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="8dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/circleProfileImage"
                app:layout_constraintBottom_toTopOf="@id/logoutButton"
                >

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    tools:text="Name"
                    android:textSize="36sp"
                    style="@style/BoldText"
                    android:text="@{viewmodel.userInfo.displayName}"
                    />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:layout_marginTop="12dp"
                    tools:text="Status"
                    style="@style/MessageSeen"
                    android:textSize="16sp"
                    android:text="@{viewmodel.userInfo.status}"
                    />


                <Button
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:layout_marginTop="62dp"
                    android:layout_marginStart="24dp"
                    android:layout_marginEnd="24dp"
                    android:text="@string/change_image"
                    android:textSize="14sp"
                    android:background="@drawable/border_corner"
                    android:onClick="@{() -> viewmodel.changeUserImagePressed()}"
                    />

                <Button
                    android:id="@+id/changeStatusBtn"
                    android:layout_width="match_parent"
                    android:layout_height="55dp"
                    android:layout_marginTop="12dp"
                    android:layout_marginStart="24dp"
                    android:layout_marginEnd="24dp"
                    android:text="@string/change_status"
                    android:textSize="14sp"
                    android:background="@drawable/border_corner"
                    android:onClick="@{() -> viewmodel.changeUserStatusPressed()}"
                    />



            </LinearLayout>


        </androidx.constraintlayout.widget.ConstraintLayout>

    </ScrollView>
</layout>